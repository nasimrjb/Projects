# array = ['a', 'b', 'c', 'd', 'e']


# def returnpairs(array):
#     for i in range(len(array)):
#         for j in range(len(array)):
#             print(array[i], array[j])


# returnpairs(array)

Array1 = ['a', 'b', 'c', 'x']
Array2 = ['d', 'e', 'f', 'x']

for item in Array2:
    if item in Array1:
        